<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.showlrc">

    <!--权限申明-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 申请SD卡写入权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- 拦截号码权限 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".showlrc.MusicActivity"/>           //进度条
        <activity android:name=".localmusic.MainActivity"/>            //本地歌曲
        <activity android:name=".intmusic.MainActivity_IntMp3"/>     //搜索页面
        <activity android:name=".usercenter.function"/>              //功能发现页面/个人中心
        <activity android:name=".songlistdetails.songlistdetail"/>              //功能发现页面/个人中心
        <activity android:name=".resetpwd"/>                            //重设密码
        <activity android:name=".register"/>                            //注册
        <activity android:name=".test"/>                                //功能测试空页面
        <activity android:name=".Login">                                //登录
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>