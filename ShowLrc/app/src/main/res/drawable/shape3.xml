<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">      <!-- 黑边透明底圆角 --><!--获取验证码按钮-->
    <corners android:radius="30dip"/>   <!-- 设置圆角弧度 -->
    <solid android:color="@android:color/transparent" />   <!-- 内置填充色 -->
    <stroke android:width="1dp" android:color="#090909" /> <!-- 设置边框颜色 -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" /><!-- 设置边框与文字距离 -->
</shape>